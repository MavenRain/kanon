def g : (Ran SPi w _ Nat Nat) := (Sec SPi w n Nat [n => (Out SPi w b Nat (APt w 1) (Out SPi w a Nat (APt w n) natAdd))])
def h : (Ran SPi w _ Nat Nat) := (Sec SPi w n Nat [n => (Out SPi w _ Nat (APt w (Out SPi w _ Nat (APt w n) g)) g)])
