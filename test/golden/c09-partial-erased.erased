rec [nat]
fun f () : union nat := KLit 5
rec [nat]
fun g () : union nat := KTail (KGlobal f) []
rec [nat]
fun n () : union nat := KTail (KGlobal g) []
rec [nat]
fun local () : union nat := KLet h (KGlobal f) (KTail (KVar 0) [])
rec [nat]
fun both () : union nat := KTail (KGlobal f) []
rec [nat]
fun runtimeAlias (union nat) : union nat := KTail (KApp (KGlobal natAdd) [KLit 1]) [KVar 0]
rec [nat]
fun appliedAlias () : union nat := KTail (KGlobal runtimeAlias) [KLit 2]
